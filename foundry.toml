[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200

remappings = [
    '@openzeppelin/contracts=lib/openzeppelin-contracts/contracts'
]

[profile.ci]
fuzz = { runs = 10000 }
invariant = { runs = 1000 }
